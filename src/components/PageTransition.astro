<script>
  class PageTransitionImpl implements PageTransition {
    isAnimating: boolean;
    firstLoad: boolean;

    constructor() {
      this.isAnimating = false;
      this.firstLoad = false;
      this.init();
    }

    init() {
      // Implementation
    }

    changePage(newPage: string, bool: boolean) {
      // Implementation
    }
  }

  new PageTransitionImpl();
</script>
